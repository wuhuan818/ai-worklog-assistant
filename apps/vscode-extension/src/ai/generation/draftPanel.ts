import * as crypto from 'node:crypto';
import * as vscode from 'vscode';
import { AiSummaryDraft, AiSummaryDraftContent, AiSummaryRevision, AiSummaryReview } from '../../apiClient';

const labels: Record<keyof AiSummaryDraftContent['sections'], string> = { task_summary: '任务总结', code_changes: '代码变更', commands_and_results: '命令与结果', bug_solutions: 'Bug 解决方案', unresolved_issues: '未解决问题', todos: '待办', daily_report: '日报', knowledge_candidates: '知识候选' };
export interface ReviewPanelState { taskId: string; drafts: AiSummaryDraft[]; draft: AiSummaryDraft; revisions: AiSummaryRevision[]; selectedRevision?: AiSummaryRevision; review: { current: AiSummaryReview | null; history: AiSummaryReview[] }; }
export interface ReviewPanelActions { selectDraft(id: string): void; selectRevision(id: string): void; save(content: AiSummaryDraftContent): void; approve(revisionId: string): void; reject(revisionId: string | undefined, reason: string): void; reload(): void; }

export function summaryDraftHtml(nonce: string): string { return `<!doctype html><html><head><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}'"><style nonce="${nonce}">body{font-family:var(--vscode-font-family);padding:12px}select,button,textarea{margin:4px;max-width:100%}textarea{width:100%;min-height:100px;font-family:var(--vscode-editor-font-family)}section{border-top:1px solid var(--vscode-editorWidget-border);padding:6px 0}.meta{color:var(--vscode-descriptionForeground)}</style></head><body><h2>AI 总结审核</h2><div><label>草稿 <select id="draft"></select></label><label>版本 <select id="revision"></select></label></div><p id="meta" class="meta"></p><div><button id="edit">开始编辑</button><button id="save">保存为新 Revision</button><button id="discard">放弃未保存修改</button><button id="reload">重新加载</button><button id="approve">批准当前 Revision</button><button id="reject">拒绝当前内容</button></div><main id="content"></main><script nonce="${nonce}">const v=acquireVsCodeApi();let state;const by=id=>document.getElementById(id);const send=(type,x={})=>v.postMessage({type,...x});function options(el,items,selected,label){el.innerHTML=items.map(x=>'<option value="'+x.id+'" '+(x.id===selected?'selected':'')+'>'+label(x)+'</option>').join('')}function render(s){state=s;options(by('draft'),s.drafts,s.draft.id,x=>new Date(x.created_at||0).toLocaleString()+' · '+(x.provider||'-')+'/'+(x.model||'-'));options(by('revision'),[{id:'original',revision_number:0},...s.revisions],s.selectedRevision?.id||'original',x=>x.id==='original'?'AI 原始草稿':'Revision '+x.revision_number);by('meta').textContent=(s.review.current?'当前已批准 Revision '+s.review.current.revision_id:'待审核')+(s.review.history[0]?.status==='rejected'?' · 最近拒绝：'+(s.review.history[0].rejection_reason||''):'');const content=(s.selectedRevision?s.selectedRevision.content:s.draft.content_json||s.draft.content);by('content').innerHTML=Object.keys(s.labels).map(k=>'<section><h3>'+s.labels[k]+'</h3><textarea data-key="'+k+'" readonly>'+JSON.stringify(content.sections[k],null,2).replace(/</g,'&lt;')+'</textarea></section>').join('');}window.addEventListener('message',e=>{if(e.data.type==='state')render(e.data.state)});by('draft').onchange=e=>send('draft',{id:e.target.value});by('revision').onchange=e=>send('revision',{id:e.target.value});by('edit').onclick=()=>document.querySelectorAll('textarea').forEach(x=>x.removeAttribute('readonly'));by('discard').onclick=()=>render(state);by('reload').onclick=()=>send('reload');by('save').onclick=()=>{try{const sections={};document.querySelectorAll('textarea').forEach(x=>sections[x.dataset.key]=JSON.parse(x.value));send('save',{content:{schema_version:'ai-summary-draft/v1',sections}})}catch(e){by('meta').textContent='格式错误：'+e.message}};by('approve').onclick=()=>{if(!state.selectedRevision)return by('meta').textContent='请先保存或选择一个 Revision';send('approve',{id:state.selectedRevision.id})};by('reject').onclick=()=>{const reason=prompt('请输入拒绝原因');if(reason)send('reject',{id:state.selectedRevision?.id,reason})};</script></body></html>`; }

export class SummaryDraftPanel implements vscode.Disposable {
  private readonly disposables: vscode.Disposable[] = []; private disposed = false;
  constructor(private readonly panel: vscode.WebviewPanel, private readonly actions: ReviewPanelActions, onDisposed?: () => void) { const nonce = crypto.randomBytes(16).toString('hex'); panel.webview.options = { enableScripts: true }; panel.webview.html = summaryDraftHtml(nonce); this.disposables.push(panel.onDidDispose(() => { this.disposed = true; onDisposed?.(); }), panel.webview.onDidReceiveMessage((m: { type: string; id?: string; content?: AiSummaryDraftContent; reason?: string }) => { if (this.disposed) return; if (m.type === 'draft' && m.id) this.actions.selectDraft(m.id); if (m.type === 'revision' && m.id) this.actions.selectRevision(m.id); if (m.type === 'save' && m.content) this.actions.save(m.content); if (m.type === 'approve' && m.id) this.actions.approve(m.id); if (m.type === 'reject' && m.reason) this.actions.reject(m.id, m.reason); if (m.type === 'reload') this.actions.reload(); })); }
  get isDisposed(): boolean { return this.disposed; }
  show(state: ReviewPanelState): void { if (!this.disposed) { void this.panel.webview.postMessage({ type: 'state', state: { ...state, labels } }); this.panel.reveal(vscode.ViewColumn.Active); } }
  dispose(): void { if (this.disposed) return; this.disposed = true; this.disposables.splice(0).forEach(item => item.dispose()); this.panel.dispose(); }
}
