import * as crypto from 'node:crypto';
import * as vscode from 'vscode';
import { AiSummaryDraft, AiSummaryRevision, AiSummaryReview } from '../../apiClient';
import { contentToForm, SummaryFormState } from './formModel';

export interface ReviewPanelState { taskId: string; drafts: AiSummaryDraft[]; draft: AiSummaryDraft; revisions: AiSummaryRevision[]; selectedRevision?: AiSummaryRevision; review: { current: AiSummaryReview | null; history: AiSummaryReview[] }; }
export interface ReviewPanelActions { selectDraft(id: string): void; selectRevision(id: string): void; save(form: SummaryFormState): void; approve(revisionId: string): void; reject(revisionId: string | undefined, reason: string): void; reload(): void; }

export function summaryDraftHtml(nonce: string): string { return `<!doctype html><html><head><meta http-equiv="Content-Security-Policy" content="default-src 'none'; style-src 'nonce-${nonce}'; script-src 'nonce-${nonce}'"><style nonce="${nonce}">body{font-family:var(--vscode-font-family);padding:12px}.toolbar,select,button{margin:4px}textarea,input,select{box-sizing:border-box;width:100%;margin:4px 0;padding:6px;background:var(--vscode-input-background);color:var(--vscode-input-foreground);border:1px solid var(--vscode-input-border)}textarea{min-height:70px;font-family:var(--vscode-font-family)}section,.card{border-top:1px solid var(--vscode-editorWidget-border);padding:10px 0}.card{border:1px solid var(--vscode-editorWidget-border);padding:10px;margin:8px 0}.meta{color:var(--vscode-descriptionForeground)}details{margin:8px 0}label{display:block;margin-top:6px;font-weight:600}.list{display:flex;gap:4px;align-items:center}.list input{flex:1}.list button{width:auto}</style></head><body><h2>AI 总结审核</h2><div><label>草稿 <select id="draft"></select></label><label>版本 <select id="revision"></select></label></div><p id="meta" class="meta"></p><div class="toolbar"><button id="edit">开始编辑</button><button id="save">保存为新 Revision</button><button id="discard">放弃未保存修改</button><button id="reload">重新加载</button><button id="approve">批准当前 Revision</button><button id="reject">拒绝当前内容</button></div><main id="content"></main><script nonce="${nonce}">const v=acquireVsCodeApi(),by=id=>document.getElementById(id),send=(type,x={})=>v.postMessage({type,...x});let state,form,editing=false;const esc=x=>String(x??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const get=(obj,path)=>path.split('.').reduce((x,k)=>x?.[k],obj);const set=(obj,path,value)=>{const p=path.split('.');const key=p.pop();const parent=p.reduce((x,k)=>x[k],obj);parent[key]=value};const evidence=refs=>'<details><summary>证据来源：'+refs.length+' 条（只读）</summary><ul>'+refs.map(r=>'<li>'+esc(({vscode_task_started:'任务开始事件',vscode_task_ended:'任务结束事件',file_changed:'文件变更',file_saved:'文件保存',code_diff:'代码差异',terminal_command:'终端命令',diagnostics_changed:'诊断变更',manual_note:'手工备注'}[r.split(':')[0]]||'工作记录证据'))+'</li>').join('')+'</ul></details>';const field=(label,path,value,area=true)=>'<label>'+label+(area?'<textarea data-path="'+path+'" '+(!editing?'readonly':'')+'>'+esc(value)+'</textarea>':'<input data-path="'+path+'" value="'+esc(value)+'" '+(!editing?'readonly':'')+'>')+'</label>';const list=(label,path,items)=>'<label>'+label+'</label><div>'+items.map((x,i)=>'<div class="list"><input data-path="'+path+'.'+i+'" value="'+esc(x)+'" '+(!editing?'readonly':'')+'><button data-remove="'+path+'.'+i+'" '+(!editing?'disabled':'')+'>删除</button></div>').join('')+'<button data-add="'+path+'" '+(!editing?'disabled':'')+'>添加一项</button></div>';const card=(title,path,item,i,fields)=>'<div class="card"><h4>'+title+' '+(i+1)+'</h4>'+fields.map(f=>field(f.label,path+'.'+i+'.'+f.key,item[f.key],f.area)).join('')+(fields.some(f=>f.key==='status')?'':'')+evidence(item.refs||item.evidence_refs||[])+'<button data-remove="'+path+'.'+i+'" '+(!editing?'disabled':'')+'>删除此项</button></div>';function objectSection(title,path,items,fields,blank){return '<section><h3>'+title+'</h3>'+items.map((x,i)=>card(title,path,x,i,fields)).join('')+'<button data-add-object="'+path+'" data-blank="'+esc(JSON.stringify(blank))+'" '+(!editing?'disabled':'')+'>添加'+title+'</button></section>'}function render(){const s=form;by('content').innerHTML='<section><h3>任务总结</h3>'+field('总结内容','task_summary.summary',s.task_summary.summary)+list('完成结果','task_summary.outcomes',s.task_summary.outcomes)+evidence(s.task_summary.refs)+'</section>'+objectSection('代码变更','code_changes',s.code_changes,[{key:'path',label:'文件或模块',area:false},{key:'summary',label:'变更内容'},{key:'impact',label:'影响'}],{path:'',summary:'',impact:'',refs:[]})+objectSection('命令与结果','commands_and_results',s.commands_and_results,[{key:'command',label:'命令'},{key:'result',label:'结果'},{key:'status',label:'状态',area:false}],{command:'',result:'',status:'unknown',refs:[]})+objectSection('Bug 解决方案','bug_solutions',s.bug_solutions,[{key:'bug_ref',label:'Bug 标识',area:false},{key:'problem',label:'问题'},{key:'solution',label:'解决方案'},{key:'verification',label:'验证'}],{bug_ref:'',problem:'',solution:'',verification:'',refs:[]})+objectSection('未解决问题','unresolved_issues',s.unresolved_issues,[{key:'issue',label:'问题'},{key:'impact',label:'影响'},{key:'next_step',label:'下一步'}],{issue:'',impact:'',next_step:'',refs:[]})+objectSection('待办','todos',s.todos,[{key:'item',label:'待办事项'},{key:'priority',label:'优先级',area:false},{key:'rationale',label:'原因'}],{item:'',priority:'medium',rationale:'',refs:[]})+'<section><h3>日报</h3>'+field('标题','daily_report.title',s.daily_report.title,false)+field('正文','daily_report.body',s.daily_report.body)+list('亮点','daily_report.highlights',s.daily_report.highlights)+list('阻塞项','daily_report.blockers',s.daily_report.blockers)+list('下一步重点','daily_report.next_focus',s.daily_report.next_focus)+evidence(s.daily_report.refs)+'</section>'+objectSection('知识候选','knowledge_candidates',s.knowledge_candidates,[{key:'title',label:'标题',area:false},{key:'category',label:'分类',area:false},{key:'summary',label:'摘要'},{key:'why_reusable',label:'可复用原因'}],{title:'',category:'',summary:'',why_reusable:'',refs:[]});document.querySelectorAll('[data-path]').forEach(x=>x.oninput=e=>set(form,e.target.dataset.path,e.target.value));document.querySelectorAll('[data-remove]').forEach(x=>x.onclick=()=>{const p=x.dataset.remove.split('.');const i=Number(p.pop());get(form,p.join('.')).splice(i,1);render()});document.querySelectorAll('[data-add]').forEach(x=>x.onclick=()=>{get(form,x.dataset.add).push('');render()});document.querySelectorAll('[data-add-object]').forEach(x=>x.onclick=()=>{get(form,x.dataset.addObject).push(JSON.parse(x.dataset.blank));render()})}function options(el,items,selected,label){el.innerHTML=items.map(x=>'<option value="'+x.id+'" '+(x.id===selected?'selected':'')+'>'+esc(label(x))+'</option>').join('')}function load(s){state=s;editing=false;form=s.form;options(by('draft'),s.drafts,s.draft.id,x=>new Date(x.created_at||0).toLocaleString()+' · '+(x.provider||'-')+'/'+(x.model||'-'));options(by('revision'),[{id:'original',revision_number:0},...s.revisions],s.selectedRevision?.id||'original',x=>x.id==='original'?'AI 原始草稿':'Revision '+x.revision_number);by('meta').textContent=s.review.current?'当前已批准 Revision '+s.review.current.revision_id:'待审核';render()}window.addEventListener('message',e=>{if(e.data.type==='state')load(e.data.state)});by('draft').onchange=e=>send('draft',{id:e.target.value});by('revision').onchange=e=>send('revision',{id:e.target.value});by('edit').onclick=()=>{editing=true;render()};by('discard').onclick=()=>load(state);by('reload').onclick=()=>send('reload');by('save').onclick=()=>{if(!editing)return;send('save',{form})};by('approve').onclick=()=>{if(!state.selectedRevision)return by('meta').textContent='请先保存或选择一个 Revision';send('approve',{id:state.selectedRevision.id})};by('reject').onclick=()=>{const reason=prompt('请输入拒绝原因');if(reason)send('reject',{id:state.selectedRevision?.id,reason})};</script></body></html>`; }

export class SummaryDraftPanel implements vscode.Disposable {
  private readonly disposables: vscode.Disposable[] = []; private disposed = false;
  constructor(private readonly panel: vscode.WebviewPanel, private readonly actions: ReviewPanelActions, onDisposed?: () => void) {
    const nonce = crypto.randomBytes(16).toString('hex'); panel.webview.options = { enableScripts: true };
    const publishingTools = `<script nonce="${nonce}">const toolbar=document.querySelector('.toolbar');for(const [id,label] of [['exportSummary','导出任务总结'],['exportDaily','导出日报'],['publishKnowledge','发布知识候选']]){const b=document.createElement('button');b.id=id;b.textContent=label;b.disabled=true;b.onclick=()=>v.postMessage({type:id,taskId:state?.taskId});toolbar.appendChild(b)}window.addEventListener('message',e=>{if(e.data.type==='state'){const allowed=!!e.data.state.review.current;['exportSummary','exportDaily','publishKnowledge'].forEach(id=>document.getElementById(id).disabled=!allowed)}});</script>`;
    panel.webview.html = summaryDraftHtml(nonce).replace('</body>', `${publishingTools}</body>`);
    this.disposables.push(panel.onDidDispose(() => { this.disposed = true; onDisposed?.(); }), panel.webview.onDidReceiveMessage((m: { type: string; id?: string; taskId?: string; form?: SummaryFormState; reason?: string }) => {
      if (this.disposed) return; if (m.type === 'draft' && m.id) this.actions.selectDraft(m.id); if (m.type === 'revision' && m.id) this.actions.selectRevision(m.id); if (m.type === 'save' && m.form) this.actions.save(m.form); if (m.type === 'approve' && m.id) this.actions.approve(m.id); if (m.type === 'reject' && m.reason) this.actions.reject(m.id, m.reason); if (m.type === 'exportSummary' && m.taskId) void vscode.commands.executeCommand('aiWorklog.exportApprovedSummary', m.taskId); if (m.type === 'exportDaily' && m.taskId) void vscode.commands.executeCommand('aiWorklog.exportApprovedDailyReport', m.taskId); if (m.type === 'publishKnowledge' && m.taskId) void vscode.commands.executeCommand('aiWorklog.publishKnowledgeCandidates', m.taskId); if (m.type === 'reload') this.actions.reload();
    }));
  }
  get isDisposed(): boolean { return this.disposed; }
  show(state: ReviewPanelState): void { if (!this.disposed) { const content = state.selectedRevision?.content || state.draft.content_json || state.draft.content; if (content) void this.panel.webview.postMessage({ type: 'state', state: { ...state, form: contentToForm(content) } }); this.panel.reveal(vscode.ViewColumn.Active); } }
  dispose(): void { if (this.disposed) return; this.disposed = true; this.disposables.splice(0).forEach(item => item.dispose()); this.panel.dispose(); }
}
